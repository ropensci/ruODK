% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ru_http.R
\name{ru_http_request}
\alias{ru_http_request}
\title{Perform one HTTP request against ODK Central.}
\usage{
ru_http_request(
  verb,
  url,
  path,
  query = NULL,
  accept = "application/json",
  headers = NULL,
  un = NULL,
  pw = NULL,
  body = NULL,
  encode = NULL,
  retries = get_retries()
)
}
\arguments{
\item{verb}{(character) The HTTP verb, e.g. \code{"GET"}, \code{"POST"}.}

\item{url}{(character) The base URL of the ODK Central server.}

\item{path}{(character) The request path, e.g. \code{"v1/projects"}.}

\item{query}{(list) Optional query string parameters.
Default: \code{NULL} (no query string).}

\item{accept}{(character) The \code{Accept} header value.
Default: \code{"application/json"}.}

\item{headers}{(character) Optional extra headers as a named character
vector, e.g. \code{c("X-Extended-Metadata" = "true")}.
Default: \code{NULL} (no extra headers).}

\item{un}{(character) The ODK Central username for basic authentication.
Default: \code{NULL} (no authentication).}

\item{pw}{(character) The ODK Central password for basic authentication.
Default: \code{NULL} (no authentication).}

\item{body}{The request body, passed on to \code{httr::RETRY()}.
Default: \code{NULL} (no body).}

\item{encode}{(character) The body encoding, passed on to \code{httr::RETRY()}.
Default: \code{NULL} (httr default).}

\item{retries}{The number of attempts to retrieve a web resource.

This parameter is given to \code{\link[httr]{RETRY}(times = retries)}.

Default: 3.}
}
\value{
The \code{httr} response object, unmodified.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
This is the centralised request helper for ruODK.
It builds an \code{httr::RETRY()} call from plain data (verb, path, query,
headers, credentials, body) and returns the \code{httr} response unchanged,
so \code{yell_if_error()} and \code{httr::content()} keep working downstream.
Future work (issue #154) replaces the \code{httr} engine inside this single
function with \code{httr2}, without touching the callers.
}
\examples{
\dontrun{
# See vignette("setup") for setup and authentication options
# ruODK::ru_setup(svc = "....svc", un = "me@email.com", pw = "...")

resp <- ruODK:::ru_http_request(
  "GET",
  url = get_test_url(),
  path = "v1/projects",
  un = get_test_un(),
  pw = get_test_pw()
)

httr::status_code(resp)
}
}
\seealso{
Other utilities:
\code{\link[=attachment_get]{attachment_get()}},
\code{\link[=attachment_link]{attachment_link()}},
\code{\link[=attachment_url]{attachment_url()}},
\code{\link[=drop_null_coords]{drop_null_coords()}},
\code{\link[=form_schema_parse]{form_schema_parse()}},
\code{\link[=get_one_attachment]{get_one_attachment()}},
\code{\link[=get_one_submission]{get_one_submission()}},
\code{\link[=get_one_submission_att_list]{get_one_submission_att_list()}},
\code{\link[=get_one_submission_audit]{get_one_submission_audit()}},
\code{\link[=handle_ru_attachments]{handle_ru_attachments()}},
\code{\link[=handle_ru_datetimes]{handle_ru_datetimes()}},
\code{\link[=handle_ru_geopoints]{handle_ru_geopoints()}},
\code{\link[=handle_ru_geoshapes]{handle_ru_geoshapes()}},
\code{\link[=handle_ru_geotraces]{handle_ru_geotraces()}},
\code{\link[=isodt_to_local]{isodt_to_local()}},
\code{\link[=odata_submission_rectangle]{odata_submission_rectangle()}},
\code{\link[=predict_ruodk_name]{predict_ruodk_name()}},
\code{\link[=prepend_uuid]{prepend_uuid()}},
\code{\link[=split_geopoint]{split_geopoint()}},
\code{\link[=split_geoshape]{split_geoshape()}},
\code{\link[=split_geotrace]{split_geotrace()}},
\code{\link[=strip_uuid]{strip_uuid()}},
\code{\link{tidyeval}},
\code{\link[=unnest_all]{unnest_all()}}
}
\concept{utilities}
\keyword{internal}
