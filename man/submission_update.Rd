% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/submission_update.R
\name{submission_update}
\alias{submission_update}
\title{Update Submission data.}
\usage{
submission_update(
  iid,
  pid = get_default_pid(),
  fid = get_default_fid(),
  xml = NULL,
  url = get_default_url(),
  un = get_default_un(),
  pw = get_default_pw(),
  retries = get_retries()
)
}
\arguments{
\item{iid}{The \code{instance_id}, a UUID, as returned by
\code{\link{submission_list}}.}

\item{pid}{The numeric ID of the project, e.g.: 2.

Default: \code{\link{get_default_pid}}.

Set default \code{pid} through \code{ru_setup(pid="...")}.

See \code{vignette("Setup", package = "ruODK")}.}

\item{fid}{The alphanumeric form ID, e.g. "build_Spotlighting-0-8_1559885147".

Default: \code{\link{get_default_fid}}.

Set default \code{fid} through \code{ru_setup(fid="...")}.

See \code{vignette("Setup", package = "ruODK")}.}

\item{xml}{(character) The complete new Submission XML as a single
string, including the \code{deprecatedID} metadata node.}

\item{url}{The ODK Central base URL without trailing slash.

Default: \code{\link{get_default_url}}.

Set default \code{url} through \code{ru_setup(url="...")}.

See \code{vignette("Setup", package = "ruODK")}.}

\item{un}{The ODK Central username (an email address).
Default: \code{\link{get_default_un}}.
Set default \code{un} through \code{ru_setup(un="...")}.
See \code{vignette("Setup", package = "ruODK")}.}

\item{pw}{The ODK Central password.
Default: \code{\link{get_default_pw}}.
Set default \code{pw} through \code{ru_setup(pw="...")}.
See \code{vignette("Setup", package = "ruODK")}.}

\item{retries}{The number of attempts to retrieve a web resource.

This parameter is given to \code{\link[httr]{RETRY}(times = retries)}.

Default: 3.}
}
\value{
A list with the updated Submission's metadata as per the ODK
Central API docs.
Top level list elements are renamed from ODK's \code{camelCase} to
\code{snake_case}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
You can use this endpoint to submit updates to an existing Submission.
The \code{instanceID} submitted with the initial version of the Submission
is used permanently to reference that Submission logically, which is to
say the initial Submission and all its subsequent versions.
Each subsequent version also provides its own \code{instanceID}.

To perform an update, provide in the Submission XML an additional
\code{deprecatedID} metadata node with the \code{instanceID} of the particular
and current Submission version you are replacing.
If the \code{deprecatedID} you give is anything other than the identifier of
the current version of the Submission at the time the server receives
it, Central answers 409.
\code{deprecatedID} must contain the current \code{instanceID}.
\code{submission_get()} returns the current \code{instanceID}.
The new version must have a new \code{instanceID}.

The XML data you send replaces the existing data entirely.
All of the data must be present in the updated XML.

When you create a new Submission version, any uploaded media files
attached to the current version that match expected attachment names
in the new version are automatically copied over to the new version.

This endpoint requires ODK Central v1.2 or later.
}
\examples{
\dontrun{
# See vignette("setup") for setup and authentication options
# ruODK::ru_setup(svc = "....svc", un = "me@email.com", pw = "...")

sl <- submission_list()
iid <- sl$instance_id[[1]]

xml <- paste0(
  '<data id="simple" version="1">',
  "<meta>",
  "<instanceID>uuid:85cb9aff-005e-4edd-9739-dc9c1a829c45</instanceID>",
  "<deprecatedID>", iid, "</deprecatedID>",
  "</meta>",
  "<name>Jo updated</name>",
  "</data>"
)

s <- submission_update(iid = iid, xml = xml)

s$current_version$instanceId
# > "uuid:85cb9aff-005e-4edd-9739-dc9c1a829c45"
}
}
\seealso{
\url{https://docs.getodk.org/central-api-submission-management/#updating-submission-data}

Other submission-management:
\code{\link[=attachment_delete]{attachment_delete()}},
\code{\link[=attachment_list]{attachment_list()}},
\code{\link[=attachment_upload]{attachment_upload()}},
\code{\link[=encryption_key_list]{encryption_key_list()}},
\code{\link[=submission_audit_get]{submission_audit_get()}},
\code{\link[=submission_changes]{submission_changes()}},
\code{\link[=submission_comment_create]{submission_comment_create()}},
\code{\link[=submission_comment_list]{submission_comment_list()}},
\code{\link[=submission_create]{submission_create()}},
\code{\link[=submission_delete]{submission_delete()}},
\code{\link[=submission_detail]{submission_detail()}},
\code{\link[=submission_export]{submission_export()}},
\code{\link[=submission_get]{submission_get()}},
\code{\link[=submission_list]{submission_list()}},
\code{\link[=submission_restore]{submission_restore()}},
\code{\link[=submission_review]{submission_review()}},
\code{\link[=submission_submitters]{submission_submitters()}},
\code{\link[=submission_version_detail]{submission_version_detail()}},
\code{\link[=submission_versions]{submission_versions()}}
}
\concept{submission-management}
